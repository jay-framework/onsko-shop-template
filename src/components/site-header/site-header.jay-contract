name: site-header
tags:
  - tag: headerHidden
    type: variant
    dataType: boolean
    phase: fast+interactive
    description: Whether the header is hidden due to scroll position
  - tag: headerHasScrolled
    type: variant
    dataType: boolean
    phase: fast+interactive
    description: Whether the user has scrolled past the header hide threshold (controls re-show animation)
